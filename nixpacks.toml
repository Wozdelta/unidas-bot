[phases.setup]
nixPkgs = ["python3", "chromium", "xvfb-run"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "xvfb-run -a python monitor_bot.py"
